# Copy this to apps/web/.env.local (Next.js loads .env from the app root, not the repo root)

# Vercel feature flags (optional; when missing, flags use defaults and app still runs)
# Set for Flags Explorer overrides in toolbar. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# FLAGS_SECRET=
